import { VStack, Heading, Progress, Button, Box } from "@chakra-ui/react";
import Timer from "./Timer";

const RightSidebar = () => {
  return <VStack p={4} align="self-end"></VStack>;
};

export default RightSidebar;
